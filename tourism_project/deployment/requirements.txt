# DataFrame construction for incoming Streamlit form values.
pandas==2.2.3
# Preprocessing pipeline classes required when loading the serialized model.
scikit-learn==1.8.0
# XGBoost runtime required by the serialized classifier.
xgboost==3.1.3
# Joblib runtime required to deserialize the model pipeline.
joblib==1.5.3
# Web-app framework used by Streamlit Community Cloud.
streamlit==1.43.2
