# Data manipulation package used throughout registration, preparation, training, and deployment.
pandas==2.2.3
# Machine-learning utilities used for preprocessing, pipelines, tuning, and evaluation.
scikit-learn==1.8.0
# Gradient-boosted tree classifier used as the final tuned model.
xgboost==3.1.3
# Model serialization package used to save and load the full fitted pipeline.
joblib==1.5.3
# Experiment tracking package installed in GitHub Actions for tuned-parameter and metric logging.
mlflow==3.0.1
